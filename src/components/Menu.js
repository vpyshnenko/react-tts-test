import React from "react";
import { Box, Icon } from "gestalt";

const Menu = () => (
  <Box marginRight={1}>
    <Icon
      size={22}
      icon="menu"
      accessibilityLabel="Menu"
      color="darkGray"
    />
  </Box>
);

export default Menu;
